/**
 * Validation functions for connector forms
 */

// Since we only have view functionality, validation is minimal
// This is kept for consistency with the pattern but not actively used
export const validateConnectorForm = (formData, setError) => {
  // For view-only mode, no validation needed
  // This function exists for consistency with the pattern
  return true;
};  